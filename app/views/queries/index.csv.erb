id|<% for column in Query.content_columns %><%= column.human_name %>|<% end %>
<% for query in @queries %><%= query.id %>|<% for column in Query.content_columns %><%= query.send(column.name).is_a?(Time) ? h(query.send(column.name).to_s(:db)) : h(query.send(column.name)) %>|<% end %>
<% end %>