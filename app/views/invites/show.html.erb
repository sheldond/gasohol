<p>
  <b>Code:</b>
  <%=h @invite.code %>
</p>

<p>
  <b>Available:</b>
  <%=h @invite.available %>
</p>

<p>
  <b>Used:</b>
  <%=h @invite.used %>
</p>

<p>
  <b>Last used at:</b>
  <%=h @invite.last_used_at %>
</p>

<h2>Users signed up with this code:</h2>
<%= render :partial => 'users/list', :locals => { :users => @invite.users } %>
<!--
<ol class="list">
  <% @invite.users.each do |user| %>
    <li><%= link_to(user.name, user_path(user.id)) %></li>
  <% end %>
</ol>
-->

<%= link_to 'Edit', edit_invite_path(@invite) %> |
<%= link_to 'Back', invites_path %>
